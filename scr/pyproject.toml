[project]
name = "drifter-scripts"
version = "0.1.0"
description = "Visualization scripts for DRIFTER"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.7",
    "numpy>=1.24",
    "pandas>=2.0",
    "scipy>=1.10",
]

[tool.uv]
dev-dependencies = []
